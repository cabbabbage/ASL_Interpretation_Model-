from ASL import ASL
from keyboard import keyboard
from consent import consent
import time
import _tkinter as tk

create_user_csv_header(consent_form, device)

#create file in ./subjects file name = consent_form.id 
#row 0 = consent.id consent_form.id, consent_form.,   



selection = tk.selection 
if  selection = asl:
    devivce = ASL()


else:

compare(result, target, input_time):


def trial_run(device, word_set):
    i = 0
    input_time
    target_word = word_set[i]:

    device.start(target_word):
    Processed = 0
    while true:
        if not device.get_result() == "":
            if compare_log(device.result, target_word)
                i = i +1
                target_word= words[i]
                device_start(target_word)
                if i = 10:
                    break
                else:
                    device.start(target_word)
            else:
                
        else 
            wait (20)
        
            



            